# 🤖 ByBit Arbitrage Bot

Автоматический бот для поиска арбитражных возможностей между биржей Bybit и обменниками (BestChange, ChangeNOW, Swapzone).

## 📋 Описание

Бот ищет арбитражные возможности по схеме:
```
Bybit (купить монету A) → Обменник (обменять A→B) → Bybit (продать монету B)
```

### Особенности:
- ✅ Учет всех комиссий (торговые, вывод)
- ✅ Фильтрация по ликвидности и объему
- ✅ Параллельная обработка запросов
- ✅ Real-time мониторинг 24/7
- ✅ Логирование всех возможностей
- ✅ Telegram уведомления (опционально)
- ✅ WebSocket для обновления цен (опционально)

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Настройка конфигурации

Создайте файл `.env` в корне проекта:

```env
# Обязательно
BESTCHANGE_API_KEY=ваш_ключ_bestchange

# Опционально (для точных комиссий на вывод)
BYBIT_API_KEY=ваш_ключ_bybit
BYBIT_API_SECRET=ваш_секрет_bybit

# Опционально (для Telegram уведомлений)
TELEGRAM_BOT_TOKEN=ваш_токен_бота
TELEGRAM_CHAT_ID=ваш_chat_id

# Параметры поиска
START_AMOUNT=100.0
MIN_SPREAD=0.1
MIN_PROFIT_USD=0.5
MONITORING_INTERVAL=20.0
```

### 3. Получение API ключей

#### BestChange API
1. Зарегистрируйтесь на [BestChange](https://www.bestchange.com)
2. Получите API ключ на [странице API](https://www.bestchange.com/wiki/api.html)
3. Добавьте ключ в `.env` как `BESTCHANGE_API_KEY`

#### Bybit API (опционально)
1. Зарегистрируйтесь на [Bybit](https://www.bybit.com)
2. Создайте API ключ в настройках аккаунта
3. Добавьте ключ и секрет в `.env`

#### Telegram Bot (опционально)
1. Создайте бота через [@BotFather](https://t.me/botfather)
2. Получите токен бота
3. Узнайте свой chat_id (можно через [@userinfobot](https://t.me/userinfobot))
4. Добавьте в `.env`

### 4. Запуск бота

```bash
python continuous_monitor.py
```

## ⚙️ Конфигурация

Все параметры настраиваются через переменные окружения в `.env`:

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `START_AMOUNT` | Начальная сумма для расчета (USD) | 100.0 |
| `MIN_SPREAD` | Минимальный спред для вывода (%) | 0.1 |
| `MIN_PROFIT_USD` | Минимальная прибыль (USD) | 0.5 |
| `MONITORING_INTERVAL` | Интервал между проверками (сек) | 20.0 |
| `MIN_24H_VOLUME_USDT` | Минимальный объем торгов 24ч (USD) | 100000 |
| `MIN_LIQUIDITY_SCORE` | Минимальная оценка ликвидности | 40.0 |
| `USE_ONLY_TOP_LIQUID_COINS` | Количество топ монет для анализа | 200 |
| `MAX_CONCURRENT_REQUESTS` | Параллельных запросов | 100 |
| `REQUEST_DELAY` | Задержка между запросами (сек) | 0.05 |
| `WEBSOCKET_ENABLED` | Использовать WebSocket для цен | True |

## 📊 Структура проекта

```
ByBit_Arbit/
├── continuous_monitor.py      # Главный модуль мониторинга
├── exchange_arbitrage_analyzer.py  # Анализатор арбитража
├── bybit_handler.py           # Клиент для Bybit API
├── bestchange_handler.py      # Клиент для BestChange API
├── opportunity_logger.py      # Логирование возможностей
├── telegram_notifier.py       # Telegram уведомления
├── configs_continuous.py      # Конфигурация
├── handlers/                  # Дополнительные обменники
│   ├── binance_handler.py
│   ├── changenow_handler.py
│   └── swapzone_handler.py
├── logs/                      # Логи (создается автоматически)
├── results/                   # Результаты (создается автоматически)
├── requirements.txt           # Зависимости
└── README.md                  # Этот файл
```

## 📝 Логирование

Бот автоматически сохраняет все найденные возможности в:
- `logs/opportunities.log` - JSON лог всех возможностей
- `logs/daily_summary.log` - Дневная статистика

### Экспорт в CSV

```python
from opportunity_logger import OpportunityLogger

logger = OpportunityLogger()
logger.export_to_csv(hours=24)  # Экспорт за последние 24 часа
```

## 🔔 Telegram уведомления

После настройки Telegram бота, бот будет отправлять уведомления:
- О новых арбитражных возможностях (если спред > 2x MIN_SPREAD)
- О новых рекордах по спреду
- Об ошибках в работе

## 🛠️ Разработка

### Запуск в режиме разработки

```bash
# Включить DEBUG режим
export DEBUG=True
python continuous_monitor.py
```

### Тестирование конфигурации

```bash
python configs_continuous.py
```

## ⚠️ Важные замечания

1. **API лимиты**: Не устанавливайте слишком маленькие задержки между запросами, чтобы не получить rate limit
2. **Комиссии**: Бот учитывает комиссии, но реальная прибыль может отличаться из-за проскальзывания
3. **Риски**: Арбитраж несет риски - всегда проверяйте возможности перед исполнением
4. **Безопасность**: Никогда не коммитьте `.env` файл в git!

## 🐛 Решение проблем

### Бот не находит возможности
- Проверьте, что `MIN_SPREAD` не слишком высокий
- Убедитесь, что `BESTCHANGE_API_KEY` правильный
- Проверьте интернет соединение

### Ошибки API
- Проверьте правильность API ключей
- Убедитесь, что не превышены лимиты запросов
- Попробуйте увеличить `REQUEST_DELAY`

### Telegram не работает
- Проверьте `TELEGRAM_BOT_TOKEN` и `TELEGRAM_CHAT_ID`
- Убедитесь, что бот запущен и добавлен в чат

## 📈 Производительность

Для оптимизации производительности:
- Используйте `USE_ONLY_TOP_LIQUID_COINS` для фокуса на ликвидных монетах
- Настройте `MAX_CONCURRENT_REQUESTS` в зависимости от мощности сервера
- Включите `WEBSOCKET_ENABLED` для real-time обновлений цен

## 📄 Лицензия

Этот проект предназначен для образовательных целей. Используйте на свой риск.

## 🤝 Вклад

Предложения по улучшению приветствуются! Создавайте issues и pull requests.

## 📞 Поддержка

Если у вас есть вопросы или проблемы:
1. Проверьте [ANALYSIS_AND_IMPROVEMENTS.md](ANALYSIS_AND_IMPROVEMENTS.md) для детального анализа
2. Создайте issue в репозитории
3. Проверьте логи в папке `logs/`

---

**Удачной торговли! 🚀**

